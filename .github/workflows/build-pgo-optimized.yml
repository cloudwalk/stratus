name: Build Optimized Binary (PGO)

permissions: {}

on:
  workflow_dispatch:
    inputs:
      features:
        description: "List of features to enable"
        required: false
        default: ""
      commit_sha:
        description: "Specific commit SHA to checkout and build. If empty, builds HEAD of the branch."
        required: false
        default: ""
  push:
    branches:
      - "main"
    paths:
      - "src/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - "rust-toolchain.toml"
      - "tests/fixtures/**"
      - "static/**"
      - ".sqlx/**"

env:
  RETENTION_DAYS: 90
  BUILDS_REPO: cloudwalk/stratus-internal-builds
  BUILDS_REF: main
  BUILDS_WORKFLOW: build-pgo-optimized.yml

jobs:
  proxy_build:
    name: Dispatch internal build and mirror artifacts
    runs-on: ubuntu-22.04
    # Internal workflow can wait up to 300 minutes for profile data before building;
    # 360 minutes is the maximum a GitHub-hosted runner allows.
    timeout-minutes: 360

    concurrency:
      group: ${{ github.workflow }}-${{ github.event.inputs.commit_sha || github.sha }}-${{ github.event.inputs.features || 'default' }}
      cancel-in-progress: true

    env:
      GH_TOKEN: ${{ secrets.INTERNAL_BUILDS_TOKEN }}

    steps:
      - name: Resolve commit SHA
        id: resolve-sha
        run: |
          ref="${{ inputs.commit_sha || github.sha }}"
          sha=$(gh api "repos/cloudwalk/stratus/commits/$ref" --jq .sha)
          echo "sha=$sha" >> "$GITHUB_OUTPUT"
          echo "Building stratus ref $ref ($sha)"

      - name: Dispatch internal build workflow
        run: |
          jq -n \
            --arg ref "$BUILDS_REF" \
            --arg stratus_ref "${{ steps.resolve-sha.outputs.sha }}" \
            --arg features "${{ inputs.features }}" \
            '{ref: $ref, inputs: {stratus_ref: $stratus_ref, features: $features}}' > dispatch.json
          echo "Dispatch payload:"
          cat dispatch.json
          gh api -X POST "repos/$BUILDS_REPO/actions/workflows/$BUILDS_WORKFLOW/dispatches" --input dispatch.json
          date -u +%Y-%m-%dT%H:%M:%SZ > dispatch-time.txt

      - name: Wait for internal build to complete
        id: wait
        run: |
          set -euo pipefail
          t0=$(cat dispatch-time.txt)
          echo "Waiting for a new $BUILDS_WORKFLOW run in $BUILDS_REPO (dispatched at $t0)..."
          run_id=""
          for _ in $(seq 1 60); do
            run_id=$(gh run list --repo "$BUILDS_REPO" --workflow "$BUILDS_WORKFLOW" --event workflow_dispatch \
              --json databaseId,createdAt \
              --jq 'map(select(.createdAt >= "'"$t0"'")) | max_by(.createdAt) | .databaseId' || true)
            if [ -n "$run_id" ] && [ "$run_id" != "null" ]; then
              break
            fi
            sleep 5
          done
          if [ -z "$run_id" ] || [ "$run_id" = "null" ]; then
            echo "No $BUILDS_WORKFLOW run appeared in $BUILDS_REPO after dispatch" >&2
            exit 1
          fi
          echo "Watching internal build run $run_id"
          echo "run_id=$run_id" >> "$GITHUB_OUTPUT"
          gh run watch "$run_id" --repo "$BUILDS_REPO" --exit-status --interval 30

      - name: Download artifacts from internal build
        run: |
          sha="${{ steps.resolve-sha.outputs.sha }}"
          run_id="${{ steps.wait.outputs.run_id }}"
          gh run download "$run_id" --repo "$BUILDS_REPO" --name "stratus-optimized-$sha" --dir stratus-optimized-artifact
          gh run download "$run_id" --repo "$BUILDS_REPO" --name "checksums-optimized-$sha" --dir checksums-optimized-artifact

      - name: Upload stratus optimized
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        id: upload-str
        with:
          name: stratus-optimized-${{ steps.resolve-sha.outputs.sha }}
          path: stratus-optimized-artifact/
          if-no-files-found: error
          retention-days: ${{ env.RETENTION_DAYS }}

      - name: Upload checksums
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: checksums-optimized-${{ steps.resolve-sha.outputs.sha }}
          path: checksums-optimized-artifact/
          if-no-files-found: error
          retention-days: ${{ env.RETENTION_DAYS }}

      - name: Print outputs
        run: |
          echo "Artifact ID: ${{ steps.upload-str.outputs.artifact-id }} (stratus optimized)"
          echo "Artifact URL: ${{ steps.upload-str.outputs.artifact-url }} (stratus optimized)"
          echo "Internal build run: https://github.com/$BUILDS_REPO/actions/runs/${{ steps.wait.outputs.run_id }}"
